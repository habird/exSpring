<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<!-- 톰캣 기본 서블릿을 사용한다. 뷰(jsp)를 webapp에서 실행할 수 있고 webapp/resources폴더가 아닌 곳에 리소스(js,css,upload)폴더 생성 가능함.-->
	<default-servlet-handler/>
	
	<!-- 반드시 webapp/resources 폴더 -->
	<!-- resources mapping="/resources/**" location="/resources/" /> junit을 쓰면 에러가 난다고함,, 4.12에서 있어도 무방 -->

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- @Controller 검색할 패키지는 DispatcherServlet실행 후 검색하기 때문에 반드시 servlet-context.xml에 설정함 -->
	<context:component-scan base-package="com.board.icia.*" />
	
	<!-- tx(트랜잭션)설정: root-context.xml 설정가능 -->
	<beans:bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<beans:property name="dataSource" ref="dataSource" />
	</beans:bean>
	<!-- @Transactional 어노테이션 사용 설정 (namespace에서 tx 체크후에 설정) 반드시 servlet-context.xml에 설정함 -->
	<tx:annotation-driven transaction-manager="txManager" />
	
	<!-- 인터셉터를 이용해서 로그인 여부 판단 -->
	<interceptors>
		<interceptor>
			<!-- mapping의 모든 요청("/*")을 Controller 전에 가로챔 -->
			<mapping path="/*" />
			<!-- 가로채는 내용 중에 예외를 둠. 가로채지 않을 내용 -->
			<exclude-mapping path="/" />	
			<exclude-mapping path="/access" />	
			<exclude-mapping path="/joinfrm" />	
			<exclude-mapping path="/memberjoin" />	
			<exclude-mapping path="/boardlist" />
			<beans:bean class="com.board.icia.userClass.SessionInterceptor" />
		</interceptor>
	</interceptors>
	
</beans:beans>
